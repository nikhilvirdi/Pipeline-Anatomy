Once the PR is approved and merged, the change becomes part of the shared main branch, which is what actually kicks off the next phase.

CI Pipeline Trigger: this merge is the event that starts the CI pipeline automatically. No one manually starts a build; the merge itself fires it.
